on:
  push:
    branches: [feature/Configure-yaml-pipeline]

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy
        env:
          url: 
            ${{secrets.RENDER_HOOK}}
        run: |
          curl "$url"
          
